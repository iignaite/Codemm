# Handoff: 2026-07-09

## Summary

Large consolidation day: architecture review, learner-model foundation, bloat removal, robustness hardening, and generation resilience. All on `main`, every commit gated on unit + integration suites and full builds.

- **Review**: `docs/codemm-architecture-review.md` — verdict (activity generator, not yet a learning system), evidence, complexity table, phased plan, open product decisions (§10), and running status (§11).
- **Learner model now exists and works end-to-end**: `LocalLearnerProfile`/`ConceptMastery`/`MasterySnapshot` contracts, `learner_profile` + `concept_mastery` tables, pure deterministic mastery updates (`src/learning/mastery.ts`) folded into every graded `judge.submit`, guided generation consuming the real mastery snapshot, `learning.*` IPC exposed to the renderer.
- **Bloat removed (~6,000 lines)**: legacy `perSlotGenerator`/`legacyAdapter` path (test-only, ~2,000 lines) with its eight tests replaced by staged-pipeline coverage; session-named service API and facade; single-plugin runtime registry; dead `/chat` route; orphaned modules (`javaSource`, `javaRewrite`, `dialogueAct`, `deferDefaults`, `ambiguity`, `sampleDrivenTests`, `structuralTopics`, `routePlanner`, `traceBus`); 53 self-deprecated doc stubs + legacy `documentation/` trees; duplicate backend screenshot set; copy-local-link UI on the review page.
- **Routing/LLM**: capability inference unified per process with a parity test; Gemini registered as a provider plugin (special cases deleted); `wording` role wired; `num_ctx` cap (default 8192, `CODEMM_OLLAMA_NUM_CTX`); memory-availability-aware model selection; structured-output micro-eval after the readiness probe → models that can't emit strict JSON route as `weak` (persisted in `runtime.capabilities`).
- **Robustness**: judge containers now run with `--pids-limit/--memory/--memory-swap/--cpus/no-new-privileges` + sized tmpfs (injected at `runDocker`); versioned declarative migrations via `PRAGMA user_version` with legacy-rename tests; IPC handler boundary tests; silenced persistence failures now log via `bestEffort`.
- **Generation resilience**: the soft-fallback is a multi-rung degradation ladder (hard→medium, then topic narrowing; provably terminating); failure messages surface kept-problem counts and resume guidance.
- **First-run UX**: no-LLM errors point at LLM Settings; onboarding tour gained a model-setup step; stale env-var and `fork`/port docs corrected.

## Open / Next

1. Non-root judge containers + `--cap-drop ALL` — needs a Docker machine (host-mount permission interactions; Java `/workspace` is still `rw`).
2. Phase 3/9: `LearningPath`/`Module`/`Lesson` contracts + deterministic path engine + roadmap UI — the core 1.0 feature.
3. Product decisions pending (review doc §10): free-Gemini onboarding tier, Docker-optional browse mode, concept taxonomy (tag-keyed vs curated).
4. Per-model generation benchmark on a Docker+Ollama machine (pipeline diagnostics make this cheap).
5. Phase 8: `ExecutionEngine` inlining + thread-lifecycle transition table (deferred deliberately; live infrastructure).

## Validation

`npm run build` (contracts + backend tsc + frontend standalone), backend unit suite (72), integration suite (44; 39 pass, 5 real-LLM key-gated skips), `node --check` on all touched Electron JS. Not run here: Docker e2e and live Ollama probes (machine lacks both).
