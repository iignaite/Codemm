# Handoff: 2026-04-05

## Summary

- Landed the first production-hardening slice for local generation reliability.
- The backend now routes models per role, splits generation into explicit slot stages, persists stage-level diagnostics, and surfaces those diagnostics in the IDE.

## Iteration Log

### Iteration 1 (Per-Role LLM Routing)

- Replaced the single run-scoped LLM snapshot path with a normalized route-plan model.
- Added backend route-plan types and role selection for:
  - `dialogue`
  - `skeleton`
  - `tests`
  - `reference`
  - `repair`
  - `edit`
- Wired Electron main to build and pass `llmRoutePlan` through IPC instead of a single model snapshot.
- Added `window.codemm.llm.getRoutePlan()` so the renderer can inspect the active routing plan safely.

### Iteration 2 (Dialogue Split + Guided Policy Recompute)

- Changed the dialogue flow so the LLM path is parser-only.
- Assistant prose and confirmation prompts are now rendered deterministically in the backend session service.
- Fixed guided fallback state so pedagogy policy is recomputed after spec fallback instead of reusing stale guided state.

### Iteration 3 (Staged Slot Pipeline)

- Added a new staged slot pipeline:
  - `skeleton`
  - `tests`
  - `reference`
  - `validate`
  - `repair`
- Added typed intermediate pipeline contracts and retry policy.
- Added weak-route capability gating so weak Ollama routes fail fast on hard or multi-topic slots instead of thrashing.
- Kept deterministic Docker validation in place and limited repair to the failed artifact path.

### Iteration 4 (Run Diagnostics + IDE Surface)

- Extended generation progress events with:
  - route selection
  - stage start / finish
  - escalation
  - terminal stage failure
- Persisted sanitized route-plan summaries into run metadata.
- Extended generation diagnostics aggregation with:
  - stage timeline
  - route selections
  - LLM vs Docker timing totals
- Added a simple `Run Details` panel on the home screen so failures can be debugged without raw prompt tracing.
- Added routing-profile and per-role model controls in **LLM Settings**, plus weak-route warning copy.

### Iteration 5 (Compatibility + Verification)

- Added a legacy injected-generator seam for backend unit tests so tests that intentionally stub generation can still verify retry logic while production uses the staged pipeline.
- Updated unit tests for the new stage diagnostics and bounded-concurrency strength gate behavior.
- Verified:
  - backend TypeScript build
  - backend unit suite
  - frontend production build

### Iteration 6 (Phase 1 Modularity Audit Fixes)

- Added a new shared workspace package at `packages/shared-contracts` and moved duplicated renderer/backend contract types there:
  - generation progress events
  - LLM route-plan types
  - renderer-safe provider/runtime status DTOs
- Updated backend and frontend builds to compile shared contracts first, then switched frontend bridge/page code and backend contract exports to consume the shared module.
- Broke the backend agent-layer cycle by moving readiness policy constants into `apps/backend/src/agent/policy.ts`.
- Split backend IPC handlers out of `apps/backend/src/ipcServer.ts` into:
  - `apps/backend/src/ipc/threads.ts`
  - `apps/backend/src/ipc/activities.ts`
  - `apps/backend/src/ipc/judge.ts`
- Split Electron IPC registration out of `apps/ide/main.js` into:
  - `apps/ide/ipc/workspace.js`
  - `apps/ide/ipc/llm.js`
  - `apps/ide/ipc/threads.js`
  - `apps/ide/ipc/activities.js`
  - `apps/ide/ipc/judge.js`
- Extracted frontend bridge clients and route logic:
  - new bridge clients under `apps/frontend/src/lib/bridge/*`
  - new `useThread` and `useActivity` hooks
  - new `apps/frontend/src/lib/threads/progressReducer.ts`
  - `apps/frontend/src/app/page.tsx` and `apps/frontend/src/app/activity/[id]/page.tsx` now focus on route composition instead of owning the full orchestration flow
- Switched `apps/frontend/src/app/settings/llm/page.tsx` to shared contract types plus `llmClient`.
- Removed the duplicated renderer `generationProgress` type file.
- Replaced stale app-local HTTP/SSE/auth-era docs in `apps/backend/docs`, `apps/frontend/docs`, `apps/backend/documentation`, and `apps/frontend/documentation` with deprecation stubs that point back to the canonical current docs.
- Verified:
  - `npm run build:backend`
  - `npm run test:unit`
  - `npm run build:frontend`
  - targeted ESLint pass for the newly extracted frontend files
- Repo-wide `npm run lint` still fails because of pre-existing unrelated frontend lint errors outside this phase, including existing `no-explicit-any`, `react/no-unescaped-entities`, and `react-hooks/set-state-in-effect` findings in files such as `CenterPane.tsx`, `RightPane.tsx`, `review/page.tsx`, `utils.ts`, `specBuilderUx.ts`, `OnboardingTour.tsx`, and `useThemeMode.ts`.

### Iteration 7 (Phase 2 Structural Modularization)

- Split the old `sessionService.ts` implementation into dedicated thread services under `apps/backend/src/services/threads/`:
  - `threadConversationService.ts`
  - `threadReadinessService.ts`
  - `threadGenerationService.ts`
  - shared helpers in `shared.ts`
- Replaced `apps/backend/src/services/sessionService.ts` with a compatibility facade so existing callers keep the same contract while orchestration responsibilities are separated.
- Introduced a repository layer under `apps/backend/src/database/`:
  - `db.ts`
  - `migrations.ts`
  - `repositories/threadRepository.ts`
  - `repositories/activityRepository.ts`
  - `repositories/runRepository.ts`
- Kept the SQLite schema and legacy `session` naming intact, but moved service access onto repositories instead of the monolithic `database.ts` module.
- Collapsed generation onto a single orchestrator entrypoint:
  - `apps/backend/src/generation/orchestrator.ts` is now the main plan-to-problem flow
  - `apps/backend/src/generation/index.ts` is a thin facade
  - `apps/backend/src/generation/legacyAdapter.ts` isolates the legacy injected-generator path for compatibility and tests
- Moved shared generation logic into dedicated services under `apps/backend/src/generation/services/`:
  - normalization
  - validation
  - scaffolding
- Refactored `apps/backend/src/services/activityProblemEditService.ts` to reuse the shared generation normalization/validation services instead of carrying its own reference-artifact handling.
- Centralized LLM route-plan and capability handling in `apps/backend/src/infra/llm/runtimeService.ts`, leaving `routePlanner.ts` as a compatibility wrapper.
- Added `apps/backend/src/judge/resultFormatter.ts` so backend IPC now returns normalized execution/judge fields:
  - `formattedStdout`
  - `formattedStderr`
  - `testCaseDetails`
- Updated the activity frontend to consume backend-normalized judge output instead of parsing JUnit trees, SQL mismatch blocks, or ANSI cleanup locally.
- Tightened bridge response parsing in `useActivity.ts` and `useThread.ts` so loose IPC payloads are normalized once in the hook layer.
- Verified:
  - `npm run build:backend`
  - `npm run build:frontend`
  - `npm run test:unit`
  - `git diff --check`

### Iteration 8 (Phase 3 Architecture Hardening)

- Added canonical shared DTOs for:
  - threads
  - activities
  - judge results
  - generation diagnostics
- Added backend contract wrappers for `thread`, `activity`, `judge`, `generation`, and `llm`, while marking `contracts/session.ts` as a persistence-compatibility surface instead of the application-facing contract.
- Refactored backend/provider selection into plugin registries:
  - `apps/backend/src/infra/plugins/provider/*`
  - `apps/backend/src/infra/plugins/runtime/*`
- Kept Gemini supported through the existing compatibility path in `codemmProvider.ts`; OpenAI, Anthropic, and Ollama now resolve through the provider plugin registry.
- Wrapped the Electron local-runtime path in a runtime plugin seam:
  - `apps/ide/localLlm/plugins/RuntimePlugin.js`
  - `apps/ide/localLlm/plugins/localRuntime.js`
- Added a reusable execution layer under `apps/backend/src/engine/execution/` and routed:
  - generation slot orchestration through `ExecutionEngine`
  - thread generation workflow sequencing through `ExecutionEngine`
- Added backend observability primitives under `apps/backend/src/infra/observability/` and turned the older trace utilities into compatibility wrappers over the new layer.
- Aligned application naming toward `thread` while keeping persistence/schema naming as `session`:
  - new aliases such as `createThread`, `getThread`, `processThreadMessage`, `generateFromThread`, and `regenerateSlotFromThread`
  - `services/sessionService.ts` remains a compatibility facade for old imports
- Tightened canonical DTO usage across backend IPC and frontend bridge/hooks:
  - `threads`, `activities`, and `judge` IPC handlers now return explicit shared DTO shapes
  - frontend bridge clients now depend on shared DTOs instead of local payload mirrors
- Kept legacy generation isolated and documented:
  - `generation/legacyAdapter.ts` now explicitly documents the remaining legacy-only responsibilities
- Added architecture docs for the new extension surfaces:
  - `docs/architecture/PLUGINS.md`
  - `docs/architecture/EXECUTION.md`
  - `docs/architecture/EXTENDING.md`
- Verified:
  - `npm run build:backend`
  - `npm run build:frontend`
  - `npm run test:unit`

### Iteration 9 (Test Suite Recovery After Phase 3)

- Fixed the backend integration fixtures that still assumed the pre-pipeline single-shot generation flow.
- Added `apps/backend/test/helpers/installGenerationStub.js` to provide one staged-generation test helper that stubs:
  - dialogue parsing
  - skeleton generation
  - test artifact generation
  - reference generation / repair
  - reference validation and baseline judge behavior
- Updated the language integration suites to use the shared staged helper instead of per-file legacy prompt stubs:
  - `activityGenerationEdgeCases.test.js`
  - `cpp/activityGenerationE2e.test.js`
  - `hardIntentWeakTestsBlocked.test.js`
  - `java/activityGenerationE2e.test.js`
  - `python/activityGenerationE2e.test.js`
  - `sql/activityGenerationE2e.test.js`
- Adjusted a few integration prompts/assertions to match the current deterministic clarification path and topic parsing rules:
  - missing difficulty now asks for `difficulty_plan`
  - explicit test prompts now use the current `Topics: ...` pattern expected by deterministic parsing
- Verified:
  - `npm run test:integration`
  - `npm test`
  - `git diff --check`

## How to Run

- Dev: `npm run dev`
- Shared contracts build: `npm run build:contracts`
- Backend build: `npm run build:backend`
- Frontend build: `npm run build:frontend`
- Backend unit tests: `npm run test:unit`
- Backend integration tests: `npm run test:integration`
- Full backend test suite: `npm test`

## Verification Run

- `npm run build:backend`
- `npm run test:unit`
- `npm run build:frontend`
- `npm run test:integration`
- `npm test`

## Known Issues / Next

- Generation cancellation and admission control are still not implemented.
- The route planner supports per-role selection and escalation plumbing, but `Auto` still uses simple local-profile heuristics rather than full catalog-aware optimization.
- Warm reusable judge workers are intentionally deferred until real diagnostics prove Docker cold-start cost is the dominant latency issue.
- `apps/backend/src/generation/perSlotGenerator.ts` still exists behind `legacyAdapter.ts` for compatibility and tests; Phase 3 should reduce that surface further once the staged pipeline has broader coverage.
- The repository split is structural but still intentionally conservative: table names and legacy `session` naming remain unchanged to avoid schema churn.
- Real-provider smoke tests are still environment-gated and continue to skip without API keys or `CODEMM_RUN_REAL_PROVIDER_SMOKE=1`.
