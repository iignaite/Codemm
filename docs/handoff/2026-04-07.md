# Handoff: 2026-04-07

## Summary

- Replaced the old exception-driven generation flow with a persistent run/slot state machine.
- Generation is now `runId`-scoped end to end and recovers deterministically after crashes.
- Judge execution now returns structured failure categories and uses stricter Docker sandboxing.

## Iteration Log

### Iteration 1 (Persistent Generation State Machine)

- Added persistent generation execution tables:
  - `generation_runs`
  - `generation_slot_runs`
  - `generation_slot_transitions`
- Added shared contracts for:
  - thread terminal generation states
  - generation run status
  - generation slot stages
  - generation slot terminal outcomes
- Refactored thread generation to:
  - create a durable run before slot execution
  - seed durable slot rows
  - persist slot-stage transitions from progress events
  - derive run/thread outcomes from slot results instead of the first thrown exception
- Added startup reconciliation so stale `PENDING` / `RUNNING` generation runs are repaired on engine boot and stuck thread state is rewritten.

### Iteration 2 (Run-Scoped IPC + Frontend Correlation)

- Reworked the generation progress bus to be keyed by `runId`.
- Updated backend thread IPC so:
  - `threads.generate`
  - `threads.generateV2`
  - `threads.subscribeGeneration`
  all accept or preserve `runId`.
- Updated Electron IPC/preload and frontend bridge clients so the UI subscribes to a specific generation run before it starts.
- Updated thread loading logic to surface latest persisted generation run metadata and accept the new terminal thread states.

### Iteration 3 (Judge Hardening + Failure Classification)

- Added structured judge failure categories:
  - `COMPILE_FAILURE`
  - `TEST_FAILURE`
  - `TIME_BUDGET_EXCEEDED`
  - `OUTPUT_LIMIT_EXCEEDED`
  - `JUDGE_INFRA_FAILURE`
- Hardened Docker execution with:
  - explicit container names and forced cleanup
  - output limit detection
  - PID / CPU / memory limits
  - `no-new-privileges`
  - `cap-drop=ALL`
- Switched Java judging to compile into `/tmp/classes` so `/workspace` is mounted read-only.
- Added explicit compile-time and execution-time timeout markers inside judge commands so reference validation no longer collapses everything into a generic timeout.

### Iteration 4 (API Surface Cleanup, Tests, Docs)

- Deprecated fake targeted slot-regeneration strategies at the backend service boundary; only `retry_full_slot` is currently allowed until stage-targeted resume exists.
- Added unit coverage for:
  - run status aggregation
  - run-scoped progress isolation
  - timeout-stage judge classification
- Added integration coverage for stale-run reconciliation and stuck-thread recovery.
- Added `docs/architecture/GENERATION_STATE_MACHINE.md` and updated top-level docs to reflect the new model.

### Iteration 5 (Real-Test Bootstrap Clarification)

- Added a shared real-provider test bootstrap in `apps/backend/test/helpers/loadRealProviderAuth.js`.
- Real backend provider tests now consistently load keys from:
  - process environment
  - `apps/backend/.env`
  - repo-root `.env`
- Verified that the backend real-provider tests still require the key in the backend test process; the Electron `safeStorage` key used by Codemm-Desktop is not automatically available to backend integration tests.
- Verified the real-provider integration suite boots cleanly and skips deterministically when `CODEMM_RUN_REAL_PROVIDER_SMOKE=1` and provider keys are not present.

### Iteration 6 (Security Hardening Follow-Up)

- Added ownership validation for `threads.subscribeGeneration` so a caller-supplied `runId` must belong to the requested thread before replay or live subscription is allowed.
- Added a regression test for the new `runId` authorization check.
- Hardened Docker execution to run judge and `/run` containers as an unprivileged UID/GID instead of the default container root user.
- Updated judge temp-directory permissions so the read-only bind mount remains accessible to the unprivileged container user.
- Added the same `CODEMM_RUN_REAL_PROVIDER_SMOKE=1` gate to the full real-LLM generation E2E suite so plain integration invocations do not silently spend tokens when provider keys are present in `.env`.

### Iteration 7 (Real Provider E2E Rewire)

- Added a new aggregate real-provider generation suite at `apps/backend/test/integration/llm/realActivityGenerationE2e.test.js`.
- The aggregate suite now:
  - runs real activity generation for each available provider
  - runs a real auto-provider fallback flow by masking higher-priority providers and asserting the persisted generation run used the expected fallback provider
  - validates persisted generation state via `generation_runs`, `generation_slot_runs`, and `generation_slot_transitions`
- Added the missing OpenAI real smoke test to `realProviderIntegrations.test.js` so the smoke suite now covers OpenAI, Anthropic, and Gemini consistently.
- Reduced the default aggregate real-E2E matrix to a cost-bounded `java` / `stdout` / `count=1`; the existing `CODEMM_E2E_LANGS`, `CODEMM_E2E_STYLES`, and `CODEMM_E2E_COUNTS` env vars still expand the matrix when needed.

### Iteration 8 (E2E Test Cleanup)

- Collapsed the four duplicated stubbed language activity-generation E2E files into one matrix suite:
  - `apps/backend/test/integration/languages/activityGenerationMatrix.test.js`
- Removed redundant one-file-per-provider real E2E wrappers and kept provider scoping inside the aggregate real suite via `CODEMM_E2E_PROVIDERS`.
- Updated remaining stubbed integration tests to assert the current terminal generation states (`COMPLETED` / persisted run status) instead of the obsolete `SAVED` contract.
- Renamed the hard-intent weak-tests regression description so it matches the current state machine semantics.
- Updated backend test documentation to point at the matrix suites instead of deleted wrapper files.

### Iteration 9 (Pre-Subscription Regression Fix)

- Fixed `threads.subscribeGeneration` so a caller-generated future `runId` is allowed before the generation run has been persisted.
- Kept the cross-thread ownership check for already-persisted runs, so the earlier `runId` authorization hardening still applies to existing runs.
- Added unit coverage for the intended frontend flow: subscribe first with a future `runId`, then start generation with the same `runId`.

### Iteration 10 (Dev Generation Logging)

- Updated the Electron dev launcher so `npm run dev` defaults `CODEMM_TRACE=1` for the backend unless it is explicitly set.
- Added IDE-side formatting for `threads.generation` events so the terminal now prints readable generation progress lines during development:
  - generation start / run status
  - slot start
  - route selection
  - stage start / finish
  - escalations
  - slot summaries
  - terminal slot failures
- Added a `CODEMM_DEV_GENERATION_LOGS=0` escape hatch to silence the IDE-side formatted generation log stream while keeping the rest of dev mode unchanged.

### Iteration 11 (Java Structural Slot Timeout Fix)

- Traced the slow generation failure to the staged pipeline missing a legacy Java structural-topic guard:
  - `skeleton`, `tests`, and `reference` generation were succeeding
  - the first `validate` pass was timing out in Docker for a Java `encapsulation` slot
  - the repair stage could then regenerate the same broken reference and burn a second full validation timeout
- Restored a fast preflight check in `apps/backend/src/pipeline/slotStages.ts` before Docker validation for Java structural-topic slots:
  - reject `stdin`-driven references (`Scanner` / `System.in`) for structural OOP topics
  - assert Java structural-topic requirements against the generated reference + test suite before invoking the judge
- Added a no-op repair guard so the staged pipeline now terminates immediately when repair regenerates the same reference artifact hash/source after a validation failure instead of spending another full `validate` timeout.
- Fixed downstream failure classification so terminal slot summaries now preserve explicit kinds like `timeout` instead of degrading to `unknown`.
- Added targeted unit coverage for:
  - Java structural-topic preflight rejection
  - no-op repair detection
  - explicit terminal failure kind preservation

### Iteration 12 (Legacy Generator Removal)

- Removed the old compatibility generation branch from production orchestration:
  - deleted `apps/backend/src/generation/legacyAdapter.ts`
  - deleted `apps/backend/src/generation/perSlotGenerator.ts`
- Simplified `generateProblemsFromPlan()` so generation now always flows through the staged pipeline in production.
- Replaced the old injected `generateSingleProblem` test seam with a narrower staged-pipeline seam (`deps.runSlotPipeline`) for unit tests that need deterministic stubs.
- Migrated the still-useful generation unit coverage to the new seam:
  - `generationMultilangPipeline.test.js`
  - `guidedScaffoldingDecay.test.js`
  - `cppStarterSynthesis.test.js`
- Removed obsolete unit tests that only validated the deleted legacy branch’s retry/event behavior or direct `perSlotGenerator.ts` helpers:
  - `constraintLocking.test.js`
  - `constraintLockRetry.test.js`
  - `generationProgressEvents.test.js`
  - `generationRetryLimits.test.js`
  - `javaStructuralRetry.test.js`
  - `testStrengthGateIntegration.test.js`
- Updated current docs to stop pointing contributors at the deleted legacy generator files.

### Iteration 13 (Incomplete Runs + Execution Diagnostics)

- Rolled the canonical partial-generation status forward from `PARTIAL_SUCCESS` to `INCOMPLETE`:
  - mixed-success generation runs now finish as `INCOMPLETE`
  - mixed-success threads now surface as `INCOMPLETE`
  - mixed-success activities are persisted as `INCOMPLETE` instead of silently looking like normal completed output
- Added new execution-diagnostics persistence tables:
  - `generation_execution_attempts`
  - `generation_slot_diagnoses`
  - `generation_run_failure_cache`
- Added an in-memory validated execution bundle boundary before Docker execution so generated drafts are sanitized before the judge runs:
  - schema validation
  - API-shape checks
  - stdin / interactive-input rejection
  - rough loop / complexity-risk heuristics
- Corrected judge-result normalization and timeout metadata so execution failures now persist richer diagnostics:
  - timeout stage
  - watchdog source
  - parsed failures
  - bounded stdout / stderr snippets and hashes
- Fixed planner/spec normalization so difficulty shorthand like `easy` is not allowed to leak into `topic_tags`.
- Updated backend test execution so the backend test runner rebuilds `@codemm/shared-contracts` before TypeScript tests compile against the shared contract declarations.

### Iteration 14 (Failed-Slot Repair + Bounded Parallelism)

- Generalized the generation orchestrator so it can:
  - preserve successful slot outputs from a prior run
  - rerun only a targeted subset of failed or interrupted slots
  - execute target slots with a bounded worker pool while still rebuilding final problem/outcome ordering by `slot_index`
- Added a new thread IPC/service path to repair incomplete generations in place:
  - `threads.repairFailedSlots`
  - backend repairs now update the existing incomplete activity instead of creating a second activity record
- Added review-screen repair UX for incomplete activities:
  - the activity payload now carries `threadId`, `failedSlotIndexes`, and `failedSlotCount`
  - review mode exposes a `Repair failed slots` action that reruns only the remaining failed slots
- Added unit coverage for:
  - targeted slot reruns preserving successful slots
  - bounded parallel execution preserving deterministic output ordering

## How to Run

- Dev: `npm run dev`
- Full build: `npm run build`
- Backend unit tests for services: `npm --workspace codem-backend run test:unit -- services`
- Backend unit tests for staged-pipeline failure guards:
  - `npm --workspace codem-backend run test:unit -- pipeline/slotStages.test.js`
  - `npm --workspace codem-backend run test:unit -- generation/validationService.test.js`
- Backend generation unit suite:
  - `npm --workspace codem-backend run test:unit -- generation`
- Backend execution-bundle unit suite:
  - `npm --workspace codem-backend run test:unit -- generation/executionBundle.test.js`
- Backend orchestrator resume / parallelism unit suite:
  - `npm --workspace codem-backend run test:unit -- generation/orchestratorResume.test.js`
- Backend integration tests for DB flows: `npm --workspace codem-backend run test:integration -- database`
- Real provider smoke checks:
  - `CODEMM_RUN_REAL_PROVIDER_SMOKE=1 npm --workspace codem-backend run test:integration -- llm/realProviderIntegrations.test.js`
- Real provider activity-generation E2E across all available providers plus auto fallback:
  - `CODEMM_RUN_REAL_PROVIDER_SMOKE=1 npm --workspace codem-backend run test:integration -- llm/realActivityGenerationE2e.test.js`
- Real provider targeted activity-generation E2E:
  - `CODEMM_RUN_REAL_PROVIDER_SMOKE=1 CODEMM_E2E_PROVIDERS=openai npm --workspace codem-backend run test:integration -- llm/realActivityGenerationE2e.test.js`
- Stubbed language matrix E2E:
  - `npm --workspace codem-backend run test:integration -- languages/activityGenerationMatrix.test.js`

## Verification Run

- `npm run build`
- `npm run build:backend`
- `npm run build:frontend`
- `npm --workspace codem-backend run test:unit -- services`
- `npm --workspace codem-backend run test:unit -- pipeline/slotStages.test.js`
- `npm --workspace codem-backend run test:unit -- generation/validationService.test.js`
- `npm --workspace codem-backend run test:unit -- generation/executionBundle.test.js`
- `npm --workspace codem-backend run test:unit -- generation/orchestratorResume.test.js`
- `npm --workspace codem-backend run test:unit -- generation`
- `npm --workspace codem-backend run test:integration -- database`
- `npm --workspace codem-backend run test:integration -- llm/realProviderIntegrations.test.js`
- `npm --workspace codem-backend run test:integration -- llm/realActivityGenerationE2e.test.js`
- `npm --workspace codem-backend run test:integration -- languages/activityGenerationMatrix.test.js`

## Known Issues / Next

- Stage-targeted slot resume is still not implemented; unsupported targeted strategy names are now rejected instead of silently rerunning full generation.
- The new `INCOMPLETE` activity contract blocks normal learner launch/publish for mixed-success runs; the review screen now exposes failed-slot repair, but live repair-progress UX still routes through the existing thread-generation event stream rather than a persistence-first repair dashboard.
- The frontend still renders progress from events during active generation; longer term it should read persisted run/slot records directly for richer recovery and history views.
- Existing diagnostics still dual-write to legacy `runs` / `run_events`; that compatibility layer can be reduced once the new run tables back the UI directly.
- Generation now supports bounded slot concurrency, but resumability is still slot-level rather than true mid-stage artifact resume because hidden reference artifacts remain non-persistent by design.
